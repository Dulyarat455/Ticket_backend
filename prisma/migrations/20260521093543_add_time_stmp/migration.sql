BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TicketStatus] ADD [timeStmp] DATETIME2 NOT NULL CONSTRAINT [TicketStatus_timeStmp_df] DEFAULT CURRENT_TIMESTAMP;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
